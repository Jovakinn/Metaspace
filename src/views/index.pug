doctype html
html
    head
        title SuperBlog!
    style
    body
        h1 Welcome to SuperBlog!
        ul
            each post in posts
                li #{post.title}
                    a(href='articles/' + post.id) Read
                    a(href="update") Update
                    a(href="delete") Delete
        a(href="create") Create

